# Projek ReactJS & NodeJS

- `frontend/` : ReactJS App
- `backend/`  : NodeJS (Express) API

## Cara Menjalankan

1. Jalankan backend (NodeJS):
   ```bash
   cd backend
   npm install
   npm start
   ```
2. Jalankan frontend (ReactJS):
   ```bash
   cd frontend
   npm install
   npm start
   ```
